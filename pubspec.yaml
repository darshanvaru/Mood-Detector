name: facecam
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.0+3
  lottie: ^2.2.0
  emailjs: ^0.0.1
  image_picker: ^0.8.5+3
  http: ^0.13.5
  url_launcher: ^6.1.9
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/portrait.png
    - assets/icon.png
